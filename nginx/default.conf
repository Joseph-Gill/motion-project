server {
    listen 80;

    location /static {
        alias /static;
    }

    location /media {
        alias /media;
    }

    location / {
        resolver 127.0.0.11;
        set $target  http://app:8000;
        proxy_pass $target;
    }
}